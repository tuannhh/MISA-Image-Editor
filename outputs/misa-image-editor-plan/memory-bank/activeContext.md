# Active context

P0 continuation update 2026-09-21: the candidate now normalizes EXIF orientation in the WPF shared preview/export loader. The exact Windows runtime has `rawpy 0.27.1`, `pillow-heif 1.7.0`, `onnxruntime 1.30.0`, and `lensfunpy 1.18.0` installed, and the user-level `MISA_PYTHON` points to it.
Release verification after this update: Release build 0 warnings/0 errors, C# catalog/orientation/auto-straighten smoke passed, native CTest 1/1 passed, Python 21/21 passed, the preview bridge covered Sony ARW/Canon CR3/Nikon NEF/Canon HIF with EXIF orientation 1, and publish-folder plus extracted-ZIP startup/bridge/mask smokes passed. The previously reported CLR startup exception is fixed and publish/ZIP startup now both keep a clean error log. Candidate ZIP SHA-256: `32671F8A24877EA04C65A1FD7C6F4CD4E1EB562670E9B49B5DE3FCB5081E4AC5`. This is still a P0 candidate, not a stable release.

P0-i18n update (22/09/2026): a WPF `LocalizationService` provides persisted Vietnamese/English UI labels for the Library, Editor, and P0 dialogs, with `BILINGUAL-DOCUMENTATION.md` defining the document policy. The candidate was rebuilt; C# domain smoke and Python 22/22 passed. The initial publish smoke found a `StaticResource I18n` title parse failure, which was fixed before the replacement publish/ZIP startup smokes passed with a newline-only log. The follow-up `AppearanceService` supplies Light/Dark/System palettes, defaults to Light for readable dark text on a bright background, and persists the choice; its candidate publish/ZIP startup smokes also passed. Current candidate SHA-256: `168A0C4F3854E07961CC6C1107B69B6B3344E188BDEFDCAD1EE9A90BB6C1F64C`.
P0 technical decision update 2026-09-21: Python is now 22/22 after adding the deterministic sRGB ΔE76 baseline (PNG 0.0000; JPEG 0.5040; HEIC 0.6086; P0 export 0.5984) and real RAW lens metadata coverage. ExifRead resolves Sony A77 metadata explicitly unmatched and Nikon Z6/NIKKOR Z 24-70mm f/4 S to exact Lensfun calibration. Fresh ARW/CR3/NEF/HIF benchmarks are 10.79–13.02 MP/s with 1270.79–1335.57 MB peak working set. The decision handoff is `handoff/P0-TECHNICAL-DECISION.md`; P0 can progress to P1/P2 product work, but no stable claim is valid without user camera/lens and AI-quality evidence.
Repeatability update: `tools/run_p0_release_gate.ps1` completed all 13 Python/.NET/native steps and saved `fixtures/p0-release-gate.json` on 2026-09-21.
Cập nhật 17/09/2026.

Mục tiêu: MISA Image Editor cho Windows theo các nhóm Library, Edit, Crop, Mask; copy/paste batch ưu tiên cao nhất; export có watermark/folder cha-con.
Người dùng yêu cầu đánh giá khả thi và lập plan, kiến trúc, roadmap, memorybank, handoff; phiên này không phải yêu cầu hoàn tất toàn bộ ứng dụng.
Đã đọc 14 ảnh tham chiếu hiển thị trong hội thoại, tra nguồn kỹ thuật/profile, lập bộ tài liệu.
Đã hỏi cấu hình; người dùng trả lời “ok nhé” với phương án mặc định Windows 11 x64/RAM ≥16 GB. Đây không phải thông tin xác nhận cấu hình thực tế hoặc model máy ảnh.
Đã có P0 Python prototype và smoke tests trong outputs/misa-image-editor-p0; solution Windows .NET 10 gồm WPF desktop shell, collection dialog, Library recent-import/all-catalog views, C# recipe contract, versioned JSON catalog store và native worker contract. WPF JPEG/PNG Basic preview feedback đã nối vào recipe cho bảy trường tone; WPF mask panel đã nối chọn/xóa raster mask, local exposure và invert subject/background vào recipe/preview; native worker validation/apply cũng đã đồng bộ đủ bảy trường; recipe serialization smoke khóa tên `schema_version`/`process_version`; JsonCatalogStore giữ recipe khi re-import, cập nhật SHA-256 fingerprint khi source đổi và giữ last import qua session rỗng; Python smoke 20/20 gồm manifest hash verification, recipe-v1 raster-mask contract với invert subject/background, HEIC 8/10-bit probe, Canon PowerShot V1 HIF camera decode/render, 10 RAW fixture decode/render (4 NEF, 2 CR2, 2 ARW, 2 CR3, 1 NRW), raster local-mask application, MODNet Photographic offline mask inference và C# catalog smoke gồm reopen/last-import/target/recipe; Lensfun exact coverage 9/13 requested variants (nearest Sony 18-135 entry giữ riêng); WPF Release build 0 warning/0 error; native CMake/MSVC Release build và ctest 1/1 passed; benchmark synthetic 12MP và Canon HIF/CR3 thật đã đo. Chưa có bộ cài, HEIF/HEIC camera matrix đầy đủ, matrix camera đầy đủ, 4 lens variants, AI quality/background matrix, brush/automatic subject UI hoặc stable release.
Task tiếp: hoàn thiện các cổng còn lại của P0 (bổ sung fixture camera Sony/Canon/Nikon theo máy người dùng, lens thiếu, model AI và đo máy mục tiêu); Canon PowerShot V1 HIF 10-bit đã có fixture thật; P0-07 ADR-0001 đã chốt. UI shell hiện chỉ minh họa Library/Editor và chưa giả vờ hỗ trợ RAW/AI. Tiếp tục spike xử lý ảnh trước khi mở rộng tính năng.
Thông tin cần lấy khi phát triển: cấu hình thực, model máy ảnh/kiểu nén và file mẫu, lens IDs, watermark, phân phối nội bộ/rộng. Không phải điều kiện để đọc/dùng kế hoạch.

<Window x:Class="MisaImageEditor.Desktop.CollectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tạo collection" Width="420" Height="270" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" ResizeMode="NoResize">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="CollectionNameLabel" Text="Collection name" Margin="0,0,0,6"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Padding="6" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}"/>
        <StackPanel Grid.Row="2" Margin="0,16,0,10">
            <CheckBox x:Name="IncludeSelectedBox" Content="Include selected photos" IsChecked="True" Margin="0,4"/>
            <CheckBox x:Name="SetTargetBox" Content="Set as target collection" IsChecked="True" Margin="0,4"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CreateButton" Content="Create" Width="86" Click="Create_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="86" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
